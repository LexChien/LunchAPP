import Foundation

enum AppConstants {
  static let googlePlacesBaseURL = "https://maps.googleapis.com/maps/api"
  static let defaultRadiusMeters: Double = 1000
  static let defaultLocaleIdentifier = Locale.current.identifier
}
